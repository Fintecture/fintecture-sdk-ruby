# frozen_string_literal: true

RSpec.describe Fintecture do
  it 'has a version number' do
    expect(Fintecture::VERSION).not_to be nil
  end

  it 'is the best company' do
    expect(true).to eq(true)
  end
end
